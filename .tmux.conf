## Start tmux-sensible
## address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# increase scrollback buffer size
set -g history-limit 50000

# tmux messages are displayed for 4 seconds
set -g display-time 4000

# refresh 'status-left' and 'status-right' more often
set -g status-interval 5

# fix vim colors
set -ga terminal-overrides ",*256col*:Tc"

# emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
set -g status-keys emacs

# focus events enabled for terminals that support them
set -g focus-events on

# forward extended modified keys (e.g. modified Enter)
set -g extended-keys on

# super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on
## End tmux-sensible

# vi copy mode
set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# order by name
# bind s choose-tree -sZ -O name
# fuck dat, beast mode
bind s display-popup -E "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"
bind -n C-z resize-pane -Z
bind -n C-x switch-client -l
bind -n C-y previous-window

# Cmd+<N> -> switch to window N (sent as unique SS3 sequences by Alacritty)
set -s user-keys[0] "\eOj"
set -s user-keys[1] "\eOa"
set -s user-keys[2] "\eOb"
set -s user-keys[3] "\eOc"
set -s user-keys[4] "\eOd"
set -s user-keys[5] "\eOe"
set -s user-keys[6] "\eOf"
set -s user-keys[7] "\eOg"
set -s user-keys[8] "\eOh"
set -s user-keys[9] "\eOi"

bind -n User0 select-window -t :0
bind -n User1 select-window -t :1
bind -n User2 select-window -t :2
bind -n User3 select-window -t :3
bind -n User4 select-window -t :4
bind -n User5 select-window -t :5
bind -n User6 select-window -t :6
bind -n User7 select-window -t :7
bind -n User8 select-window -t :8
bind -n User9 select-window -t :9

# Sane scrolling
set -g mouse on

# send the prefix to client inside window
bind-key -n C-a send-prefix

# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @continuum-restore 'on'
set -g @resurrect-save-command-strategy 'pgrep'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-pane-contents-area 'visible'

# theme
set -g @plugin 'rose-pine/tmux'
set -g @rose_pine_variant 'main'
set -g @rose_pine_disable_active_window_menu 'on'
set -g @rose_pine_left_separator '  ' # The strings to use as separators are 1-space padded
set -g @rose_pine_right_separator '  ' # Accepts both normal chars & nerdfont icons
set -g @rose_pine_field_separator '  '
set -g @rose_pine_window_status_separator " | "
set -g @rose_pine_current_window_icon ' @ ' # Changes the default icon to the left of the active window name
set -g @rose_pine_session_icon 'S'

# recommended by neovim
set-option -sg escape-time 10
set-option -sa terminal-overrides ',XXX:RGB'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# run <prefix> + I to install plugins
run -b '~/.tmux/plugins/tpm/tpm'

